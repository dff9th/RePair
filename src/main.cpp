/*
#include <fstream>
#include <iostream>
#include <string>
#include <vector>

#include "rputil.hpp"
*/


int main(int argc, char **argv) {
    /*
    RpStruct rp{};
    std::string ifname{};
    ErrorCode err{};

    rp.MakeFromFile(ifname, err);

    return 0;
    */
}
