#include <fstream>
#include <iostream>
#include <string>
#include <vector>

#include "parsedarg.hpp"
//#include "rputil.hpp"

int main(int argc, char **argv) {
    //RpStruct rp{};
    //std::string ifname{};
    //ErrorCode err{};

    //rp.MakeFromFile(ifname, err);
    
    ParsedArg parg(argc, argv);

    return 0;
}
