stages:
  - build
  - test

build:
  stage: build
  tags:
    - docker
  image: rikorose/gcc-cmake:latest
  script:
    - mkdir build
    - cd build
    - cmake --version
    - cmake ..
    - make

test: 
  stage: test
  dependencies:
    - build
  script:
    - ./build/bin/test_sample

